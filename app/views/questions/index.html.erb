<h1>All Questions</h1>
<% @question.each do |questions| %>
  <div class="media">
    <div class="media-body">
      <h4 class="media-heading">
        <%= link_to question.title, question %>
      </h4>
    </div>
  </div>
  <% end %>
